apply plugin: 'war'
apply plugin: 'eclipse'
apply plugin: 'maven'

group = "com.warspite.insulae"
ext.nextRelease = '0.1.3';

if (project.hasProperty("release")) {
	version = ext.nextRelease
} 
else {
	version = ext.nextRelease + '-SNAPSHOT'
}

jar.enabled = false

artifacts {
	archives war
}
